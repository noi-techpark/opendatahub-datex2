apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "datex2.fullname" . }}
  labels:
    {{- include "datex2.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- .Values.config | toYaml | nindent 4 }}
